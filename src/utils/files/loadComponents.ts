import ObSpice from '../../main';

/**
 * Load all components in the components folder with a valid spice model
 * whilst adding the base components to the cstore.
 * @param {string} path - The path to the components folder.
 * @returns {void}
 * @example
**/
export default function loadComponents(path: string): void {
if (ObSpice.debug) console.log('Loading components...');
    

}
